<configuration name="switch.conf" description="Core Configuration">
                  
    <settings>     
        <param name="colorize-console" value="true"/>
        <!-- Default Global Log Level - value is one of debug,info,notice,warning,err,crit,alert -->
        <param name="loglevel" value="debug"/>
        <param name="crash-protection" value="false"/>
        <!-- Set the core DEBUG level (0-10) -->
        <param name="debug-level" value="0"/>
        <param name="verbose-channel-events" value="yes"/>
        <!--
            Limits
        -->    
        <param name="max-sessions" value="$${max_sessions}"/>
        <param name="max-proceeding" value="$${max_sessions}"/>
        <param name="sessions-per-second" value="$${max_cps}"/>
        <param name="min-idle-cpu" value="10"/>
        <!--
            SQL
        -->
        <param name="core-db-dsn" value="pgsql://host=$${sql_cluster_host} dbname=freeswitch user=$${sql_cluster_user} password=$${sql_cluster_password} options='-c client_min_messages=NOTICE' application_name='freeswitch'" />
        <param name="auto-create-schemas" value="true"/>
        <param name="max-db-handles" value="$${max_sessions}"/>
        <param name="db-handle-timeout" value="200"/>
        <!--
            RTP
        -->    
        <param name="rtp-enable-zrtp" value="false"/>

    </settings>

</configuration>